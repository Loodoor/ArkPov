(begin
    (defun benchmark (func arg tests)
        (begin
            (define start 0)
            (define total_time 0)

            (set! tests (if (> tests 5) tests 5))

            (until
                (? (>= tests 0))
                (begin
                    (set! start (time))
                    (func arg)
                    (set! tests (- tests 1))
                    (set! total_time (- (time) start))
                    (display total_time)
                )
            )
        )
    )
)